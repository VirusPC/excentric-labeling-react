import { Fragment } from "react"
import { Typography, Slider} from "antd"
import BottomWidgets from "./BottomWidgets";

const {Text, Row, Col} = Typography;

export default function TopWidgets(sliderParams) {
  return (
""
  )
}